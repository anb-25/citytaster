# Example dev Secret (stringData allows plain text for convenience).
# Replace or remove as needed. For serious secrets, prefer a manager (e.g., External Secrets).
# Example Secret for dev credentials/tokens. Replace with real values or use
# external secret managers in production environments.
apiVersion: v1
kind: Secret
metadata:
  name: citytaster-secrets
  namespace: citytaster-dev
stringData:
  SAMPLE_TOKEN: dev-token-value
